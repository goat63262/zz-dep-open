pull_request_rules:
  - name: zz-fork-probe (observe from-fork evaluation)
    conditions:
      - base = main
      - label = zz-forkprobe
      - -from-fork
    actions:
      label:
        add:
          - zz-treated-as-internal
